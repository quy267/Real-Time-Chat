Build the Real Time Chat Backend with the following requirements:

- Tech Stack: Python 3, Redis, WebSockets (http://Socket.io), FastAPI, Celery, Docker
- Apply TDD and Clean Architecture principles
- Use Docker for containerization
- Ensure the test coverage more than 90% with unit, integration tests and end-to-end tests
- Ensure scanned for N+1 queries and missing indexes before final submission